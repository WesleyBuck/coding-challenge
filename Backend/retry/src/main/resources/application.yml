spring:
  mail:
    host: localhost
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          port: 1025
          auth: false
          starttls:
            enable: false
            required: false
  data:
    mongodb:
      uri: mongodb://localhost:27017/db
